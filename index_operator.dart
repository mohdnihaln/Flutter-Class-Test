void main() {
  List<int> numbers = [10, 20, 30, 40];

  // Taking element using the [] operator
  print(numbers[1]);

  // Modify element using the []= operator
  numbers[1] = 25;
  print(numbers); 
}
